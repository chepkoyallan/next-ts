import { ConfigurationDashboard } from 'src/sections/configuration/configuration-dashboard';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Configuration | System Settings',
};

export default function ConfigurationPage() {
  return <ConfigurationDashboard />;
}
